* **yq (YAML):**  yq is a similar tool for YAML. It provides similar functionality to jq for processing YAML data.

**Example using yq (YAML):**

```bash
# Extract the "host" field from a YAML file
yq '.host' data.yaml
```
